export type UserType = {
  id: string;
  login: string;
  email: string;
  createdAt: string;
};

export type PostUserType = {
  login: string;
  email: string;
  createdAt: string;
};

export type PutUserType = {
  login: string;
  email: string;
  createdAt: string;
};
